class Solution {
public:
    void rotateArrayByOne(vector<int>& nums) {
        int n=nums.size();
        int temp=nums[0];
        if(n<=1) return ;
       else{ 
        for (int i=0;i<=n-2;i++){
            nums[i]=nums[i+1];
        }
        nums[n-1]=temp;}

    }
};
